// import axios from "axios";

// // const API_BASE_URL =
// //   process.env.NODE_ENV === "production"
// //     ? "https://vercel"
// //     : "http://localhost:5001";

// const API_BASE_URL = "http://localhost:5001";
// // const API_BASE_URL = "https://soar-backend.vercel.app";

// export const fetchUser = async () => {
//   return (await axios.get(`${API_BASE_URL}/user`)).data;
// };

// export const fetchCards = async () => {
//   return (await axios.get(`${API_BASE_URL}/data/cards`)).data;
// };

// export const fetchTransactions = async () => {
//   return (await axios.get(`${API_BASE_URL}/data/transactions`)).data;
// };

// export const fetchCharts = async () => {
//   return (await axios.get(`${API_BASE_URL}/data/charts`)).data;
// };
